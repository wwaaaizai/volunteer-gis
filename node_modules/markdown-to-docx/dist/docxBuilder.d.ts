import { Document } from "docx";
import type { ParsedElement } from "./markdownParser";
export declare class DocxBuilder {
    private fontFamily;
    private children;
    private numberingConfig;
    private basePath?;
    private media;
    constructor(fontFamily?: string, basePath?: string);
    addElement(element: ParsedElement): void;
    private addHeading;
    private addTable;
    private addBlockquote;
    private addCheckbox;
    private addList;
    private addParagraph;
    build(): Document;
    private addImage;
    save(outputFile: string): Promise<string>;
}
//# sourceMappingURL=docxBuilder.d.ts.map