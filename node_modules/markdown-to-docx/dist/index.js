"use strict";
// src/index.ts
// 메인 라이브러리 진입점
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.DocxBuilder = exports.parseInlineFormatting = exports.parseMarkdown = void 0;
exports.convertMarkdownToDOCX = convertMarkdownToDOCX;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const markdownParser_1 = require("./markdownParser");
Object.defineProperty(exports, "parseMarkdown", { enumerable: true, get: function () { return markdownParser_1.parseMarkdown; } });
const inlineFormatter_1 = require("./inlineFormatter");
Object.defineProperty(exports, "parseInlineFormatting", { enumerable: true, get: function () { return inlineFormatter_1.parseInlineFormatting; } });
const docxBuilder_1 = require("./docxBuilder");
Object.defineProperty(exports, "DocxBuilder", { enumerable: true, get: function () { return docxBuilder_1.DocxBuilder; } });
/**
 * 마크다운 파일을 DOCX로 변환합니다.
 * @param inputFile - 입력 마크다운 파일 경로
 * @param outputFile - 출력 DOCX 파일 경로
 * @param options - 옵션
 * @returns 생성된 파일 경로
 */
async function convertMarkdownToDOCX(inputFile, outputFile, options = {}) {
    const fontFamily = options.fontFamily || "맑은 고딕";
    const verbose = options.verbose !== false;
    // 파일 읽기
    if (!fs.existsSync(inputFile)) {
        throw new Error(`파일을 찾을 수 없습니다: ${inputFile}`);
    }
    const content = fs.readFileSync(inputFile, "utf-8");
    // 마크다운 파일 디렉토리 (이미지 상대 경로 해석용)
    const basePath = path.dirname(path.resolve(inputFile));
    // 마크다운 파싱
    const elements = (0, markdownParser_1.parseMarkdown)(content, { verbose });
    // DOCX 생성 (basePath 전달)
    const builder = new docxBuilder_1.DocxBuilder(fontFamily, basePath);
    elements.forEach((element) => {
        builder.addElement(element);
    });
    // 파일 저장
    const result = await builder.save(outputFile);
    if (verbose) {
        console.log(`✓ DOCX 파일 생성 완료: ${result}`);
        console.log(`📊 통계:`);
        console.log(`  총 요소: ${elements.length}개`);
        console.log(`  글꼴: ${fontFamily}\n`);
    }
    return result;
}
//# sourceMappingURL=index.js.map